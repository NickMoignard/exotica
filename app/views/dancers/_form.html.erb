<%= form_with(model: dancer, local: true) do |form| %>
  <% if dancer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dancer.errors.count, "error") %> prohibited this dancer from being saved:</h2>

      <ul>
      <% dancer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :stage_name %>
    <%= form.text_field :stage_name, id: :dancer_stage_name %>
  </div>

  <div class="field">
    <%= form.label :full_name %>
    <%= form.text_field :full_name, id: :dancer_full_name %>
  </div>

  <div class="field">
    <%= form.label :account %>
    <%= form.text_field :account, id: :dancer_account %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
